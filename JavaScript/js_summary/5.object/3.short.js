const x = 0;
const y = 0;
const coordiante = { x, y }; // same as { x: x, y: y };
console.log(coordiante);

function makeObj(name, age) {
  return { name, age };
  // same as return { name: name, age: age };
}
